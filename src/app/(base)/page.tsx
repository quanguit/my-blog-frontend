import { Box } from '@mui/material';

import { Icon } from '@/components';

export default function HomePage() {
  return (
    <Box mt={4}>
      <Icon name="Logo" sx={{ fontSize: 100 }} color="primary" />
    </Box>
  );
}
